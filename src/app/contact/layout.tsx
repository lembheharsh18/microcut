import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Contact Microcut Technology | Get a Machining Quote",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
